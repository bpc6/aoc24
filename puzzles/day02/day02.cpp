#include "day02.h"
#include "parsing.h"
#include <algorithm>
#include <numeric>
#include <ranges>

bool is_safe(const std::vector<int> &level) { return false; }

int part1(const std::string &filename) { return 0; }

int part2(const std::string &filename) { return 0; }
