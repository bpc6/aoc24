#include "day11.h"
#include "parsing.h"
#include <algorithm>
#include <numeric>
#include <ranges>

size_t part1(const std::string &filename) { return 0; }

size_t part2(const std::string &filename) { return 0; }
